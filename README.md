RentFlow: Микросервисная платформа управления арендой

🚀 Обзор ПроектаRentFlow — это платформа, построенная на микросервисной архитектуре, предназначенная для управления арендой. Взаимодействие между сервисами осуществляется через Docker Compose, где Django выступает как основной API-шлюз, а FastAPI обрабатывает высоконагруженные операции (например, трекинг) с использованием gRPC.

🏗️ Архитектура Системы и КомпонентыПроект состоит из следующих ключевых сервисов:
СервисТехнологияРольПортdjango-webPython/DjangoОсновной API-шлюз, бизнес-логика, ORM.8000
fastapi-trackingPython/FastAPIСервис трекинга и обработки событий, gRPC-сервер.8001
postgresPostgreSQL 16Основная реляционная база данных.5432
redisRedis 7Кэш и брокер для Celery.6379
kafkaApache Kafka 7.4.0Брокер сообщений для асинхронной связи.9092
akhqAKHQUI для мониторинга Kafka.8080
react-frontendReact/NginxФронтенд-приложение.3000

⚙️ Начало Работы (Setup)

1. ТребованияДля запуска проекта вам потребуется установленный Docker и Docker Compose.
2. Клонирование и ЗапускКлонируйте репозиторий и перейдите в корневую папку:git clone [адрес репозитория]
cd rentflow

🚨 Критический шаг: Генерация gRPC-кодаПеред запуском контейнеров необходимо сгенерировать Python-модули из файла протокола (rentflow.proto). 
Без этого шага сервис django-web упадет с ошибкой ModuleNotFoundError.
Убедитесь, что у вас установлен grpcio-tools:
pip install grpcio grpcio-tools
Запустите команду компиляции из корневого каталога rentflow:
python -m grpc_tools.protoc -I. --python_out=. --pyi_out=. --grpc_python_out=. rentflow_protos/rentflow.proto
Запустите все сервисы с помощью 
Docker Compose:docker compose up -d --build

3. Доступ к Сервисам (URLs)

СервисАдресDjango APIhttp://localhost:8000

FastAPI Trackinghttp://localhost:8001

Kafka UI (AKHQ)http://localhost:8080

React Frontendhttp://localhost:3000