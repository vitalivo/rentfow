# 🛠️ Журнал Проблем и Решений (E2E Pipeline)

Дата: 3 ноября 2025 года

Цель: Настроить сквозной (End-to-End) поток событий: Django (Producer) -> FastAPI (gRPC Server/Kafka Producer) -> Kafka -> Django Consumer.

---

## Проблема 1: Ошибка gRPC 'Internal' при вызове TerminateLease

**Описание:** При первом вызове gRPC из Django в FastAPI для метода `TerminateLease` возникала ошибка `INTERNAL` (код 13). Логи FastAPI не давали четкого ответа.
**Причина:** Неверная инициализация Kafka Producer внутри обработчика gRPC. FastAPi пытался создать Producer для каждого запроса, что приводило к конфликтам.
**Решение:** Переместили инициализацию `KafkaProducer` в **глобальную область** (или в специальный менеджер ресурсов, такой как `lifespan`) FastAPI, чтобы он был создан только один раз при запуске приложения.

---

## Проблема 2: Отсутствие логов Consumer, хотя gRPC успешен

**Описание:** gRPC-вызов был успешным, но контейнер `rentflow-django-consumer` не показывал никаких логов обработки сообщений (`LeaseTerminated`), даже после перезапуска.
**Причина 2.1 (Логирование):** Consumer использовал `logger.info()`, который не настроен по умолчанию для вывода в Docker-логи.
**Решение 2.1:** Временно заменили `logger.info()` на **`print()`** (или `self.stdout.write`) в `process_message`.

**Причина 2.2 (Consumer Group Offset):** Consumer зафиксировал смещение (offset) в Kafka на конец топика, пропустив тестовые сообщения, отправленные до его запуска.
**Решение 2.2:** Остановили Consumer (`docker stop`), принудительно удалили его группу в Kafka (`kafka-consumer-groups --delete`) и перезапустили (`docker start`), чтобы он начал читать с `earliest`.

---

## Проблема 3: Абсолютно пустые логи контейнера Consumer

**Описание:** Даже после исправления логирования и сброса Kafka, `docker logs rentflow-django-consumer` возвращал пустой вывод.
**Причина:** Проблема с буферизацией Python (stdout) внутри оболочки контейнера (`sh -c`).
**Решение:** Добавили флаг **`-u`** (unbuffered) в команду запуска в `docker-compose.yml`:
```yaml
command: python -u manage.py run_kafka_consumer

Проблема 4: SyntaxError в Consumer после исправления 3
Описание: При запуске Consumer возникла ошибка SyntaxError: unicodeescape. Причина: Случайный скопированный путь Windows был вставлен первой строкой в файл run_kafka_consumer.py. Решение: Удалили лишнюю строку с путем Windows из файла Consumer.

🔥 ИТОГ: Последовательное решение этих проблем подтвердило полную работоспособность E2E-пайплайна.