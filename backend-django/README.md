Сервис django-web: Основной API и Бизнес-логика

🌟 Роль СервисаСервис django-web выполняет роль API-шлюза и основного хранилища бизнес-логики (БЛ) в системе RentFlow.

API Gateway: Принимает входящие HTTP-запросы от фронтенда (react-frontend).

Бизнес-логика: Управляет основными моделями данных (арендаторы, договоры, недвижимость) через Django ORM.

gRPC Клиент: Инициализирует gRPC-вызовы к сервису fastapi-tracking для отправки данных (например, о создании нового договора) в реальном времени.🛠️ Среда и Конфигурация

ПараметрЗначениеОписаниеПорт8000
Порт, открываемый Docker-контейнером.
WORKDIR/appРабочая директория внутри контейнера.
Зависимостиrequirements.txtDjango, Django REST Framework, grpcio.
База данныхpostgresПодключение через Django ORM.

🚀 Запуск (через Docker Compose)
Сервис запускается автоматически командой 
docker compose up.
Команда входа в контейнер (для миграций или отладки):
docker compose exec django-web bash
Ручная команда запуска (внутри контейнера):
python manage.py runserver 0.0.0.0:8000

🔗 gRPC Взаимодействие

Сервис импортирует сгенерированные gRPC-модули (rentflow_protos) для отправки структурированных сообщений сервису fastapi-tracking.
Ключевой файл:
leases/grpc_client.py: Содержит логику инициализации gRPC-канала и вызова удаленных процедур.