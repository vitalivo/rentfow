# ⚡ FastAPI Service — RentFlow

Микросервис для real-time уведомлений и работы с событиями.

## 🚀 Назначение
- Приём событий от Django через gRPC
- Обработка и рассылка уведомлений через WebSockets
- Интеграция с Telegram и Email
- Потоковая передача событий (gRPC streaming)

## 🛠️ Технологии
| Компонент | Использование |
|----------|--------------|
| FastAPI | ASGI-фреймворк |
| Uvicorn | ASGI-сервер |
| WebSockets | Real-time обновления для фронтенда |
| Kafka | Чтение событий о платежах |
| gRPC | Приём данных от Django |
| Protobuf | Сериализация сообщений |

## 🔗 Подключения
| Сервис | Как подключается |
|-------|------------------|
| Kafka | `kafka:9092` — для чтения топиков |
| PostgreSQL | Только для чтения (через SQLAlchemy) |
| Django | Через gRPC-сервер |
| Frontend | Через WebSocket (`ws://localhost:8001/ws/notifications`) |

## 📂 Структура

src/
├── main.py # Запуск FastAPI
├── grpc_server/ # gRPC-эндпоинты
│ └── lease_service.py
├── websocket/ # Обработчики WebSocket
│ └── manager.py
└── services/ # Логика уведомлений
└── notification_service.py